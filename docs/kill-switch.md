# Binance kill switch

**VERIFIED path:** Profile → Dashboard → Sub-account → Account Management

Four UI actions (official MCP docs):

1. **Transfer** — fund or withdraw from the Agentic sub-account (human, not agent).
2. **Permissions** — view scopes. To change them: disconnect and reconnect.
3. **Disconnect agents** — select one or more agents. Reconnect later allowed.
4. **Emergency stop** — disconnect **all** connected agents **and cancel all** spot, margin, and futures positions and orders in this Agentic account.

## Honesty

Guardian quarantine is a **local hard stop** (executor disabled).  
Programmatic MCP disconnect / emergency-stop: **UNVERIFIED**. Do not claim it unless Day 0 `tools/list` proves a tool.

Demo copy: *"Guardian froze the agent. Binance kill switch is user-owned and currently UI-only."*

**Warning:** Emergency stop cancels live positions. The helper must never auto-click it. Pre-open this tab before filming.

## Captured URL (Day 0)

Paste the real Account Management URL here. The Transfer deep link in CN docs is a different page.

```
TBD
```
